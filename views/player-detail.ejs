<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= player.first_name %> <%= player.last_name %> | NBA Stats</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-gray-100 text-gray-900 font-sans">

  <div class="flex min-h-screen">
    <!-- Sidebar -->
    <%- include('partials/sidebar') %>

    <!-- Main content -->
    <main class="ml-64 flex-1 p-8 flex items-start justify-center">
      <div class="max-w-4xl w-full bg-white p-8 rounded-xl shadow-lg flex flex-col md:flex-row gap-8">
        
        <!-- Player Image -->
        <img src="<%= player.image || '../public/imgs/default.jpeg' %>" 
             alt="<%= player.first_name %> <%= player.last_name %>" 
             class="w-64 h-64 object-cover rounded-lg shadow mx-auto md:mx-0">

        <!-- Player Info -->
        <div class="flex-1">
          <h1 class="text-4xl font-bold mb-6 text-center md:text-left">
            <%= player.first_name || 'N/A' %> <%= player.last_name || '' %>
          </h1>
          
          <ul class="bg-gray-50 p-6 rounded-lg shadow space-y-2">
            <li><strong>Team:</strong> <%= player.team?.full_name || 'N/A' %></li>
            <li><strong>Position:</strong> <%= player.position || 'N/A' %></li>
            <li><strong>Height:</strong> 
              <%= player.height_feet ? player.height_feet + ' ft' : '-' %> 
              <%= player.height_inches ? player.height_inches + ' in' : '' %>
            </li>
            <li><strong>Weight:</strong> <%= player.weight_pounds ? player.weight_pounds + ' lbs' : 'N/A' %></li>
            <li><strong>College:</strong> <%= player.college || 'N/A' %></li>
            <li><strong>Draft Year:</strong> <%= player.draft_year || 'N/A' %></li>
          </ul>

          <a href="/players" class="inline-block mt-6 text-blue-600 hover:underline">← Back to Players</a>
        </div>
      </div>
    </main>
  </div>

  <script>
    // Animate player page
    gsap.from("h1", { opacity: 0, y: -30, duration: 1 });
    gsap.from("ul li", { opacity: 0, y: 20, stagger: 0.1, duration: 0.5, delay: 0.3 });
  </script>

</body>
</html>
