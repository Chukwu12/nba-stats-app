<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Search NBA Player</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-gray-100 text-gray-900 font-sans">

<div class="flex min-h-screen">
  <%- include('partials/sidebar') %>

  <main class="ml-64 flex-1 p-8 flex flex-col items-center">
    
    <!-- Search Form -->
    <form action="/players/search" method="POST" class="w-full max-w-md mb-8">
      <div class="flex gap-2">
        <input type="text" name="playerName" placeholder="Enter player name" class="flex-1 border p-2 rounded shadow">
        <button type="submit" class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700 font-semibold">Search</button>
      </div>
    </form>

    <% if (error) { %>
      <p class="text-red-600 mb-4"><%= error %></p>
    <% } %>

    <% if (player) { %>
      <!-- Player Info Card -->
      <div class="w-full max-w-4xl bg-white p-8 rounded-xl shadow-lg flex flex-col md:flex-row gap-8">
        <img src="<%= player.image %>" alt="<%= player.first_name %>" class="w-64 h-64 object-cover rounded-lg shadow mx-auto md:mx-0">

        <div class="flex-1">
          <h1 class="text-4xl font-bold mb-6 text-center md:text-left">
            <%= player.first_name %> <%= player.last_name %>
          </h1>

          <ul class="bg-gray-50 p-6 rounded-lg shadow space-y-2">
            <li><strong>Team:</strong> <%= player.team.full_name %></li>
            <li><strong>Position:</strong> <%= player.position || 'N/A' %></li>
            <li><strong>Height:</strong> <%= player.height_feet || '-' %> ft <%= player.height_inches || '-' %> in</li>
            <li><strong>Weight:</strong> <%= player.weight_pounds || 'N/A' %> lbs</li>
            <li><strong>College:</strong> <%= player.college || 'N/A' %></li>
            <li><strong>Draft Year:</strong> <%= player.draft_year || 'N/A' %></li>
          </ul>
        </div>
      </div>
    <% } %>

  </main>
</div>

<script>
  gsap.from("h1", { opacity: 0, y: -30, duration: 1 });
  gsap.from("ul li", { opacity: 0, y: 20, stagger: 0.1, duration: 0.5, delay: 0.3 });
</script>

</body>
</html>
