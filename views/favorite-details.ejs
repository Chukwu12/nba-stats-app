<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= player.first_name %> <%= player.last_name %> | NBA Stats</title>
  <script src="https://cdn.tailwindcss.com"></script>
  
   <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" /><script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
<section class="p-8 bg-gray-100 min-h-screen">
  <% if (player) { %>
    <div class="max-w-md mx-auto bg-white rounded-2xl shadow-lg p-6 text-center">
      <img src="<%= image %>" alt="<%= player.first_name %> <%= player.last_name %>" class="w-40 h-40 mx-auto rounded-full shadow-lg">
      <h2 class="text-2xl font-bold mt-4"><%= player.first_name %> <%= player.last_name %></h2>
      <p class="text-gray-600 mb-2"><%= player.team.full_name %></p>
      <p class="text-gray-700">Position: <%= player.position || 'N/A' %></p>

      <div class="grid grid-cols-3 gap-4 mt-6 text-center">
        <div><p class="font-semibold">Height</p><p><%= player.height_feet || 'N/A' %>' <%= player.height_inches || '' %>"</p></div>
        <div><p class="font-semibold">Weight</p><p><%= player.weight_pounds || 'N/A' %> lbs</p></div>
        <div><p class="font-semibold">PPG</p><p><%= stats.pts || 'N/A' %></p></div>
      </div>
    </div>
  <% } else { %>
    <p class="text-center text-gray-500">Player details not available.</p>
  <% } %>
</section>

  


</body>

</html>