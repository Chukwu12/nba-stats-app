<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
<title> Player Details</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- Tailwind CSS -->
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
</head>

<body class="bg-gray-100 font-sans min-h-screen flex items-center justify-center p-4">

  <% if (player) { %>
    <div class="bg-white rounded-xl shadow-lg p-8 max-w-md text-center w-full">
      <img src="<%= image %>" alt="<%= player.first_name %> <%= player.last_name %>" class="w-40 h-40 mx-auto rounded-full shadow-md">
      <h1 class="text-3xl font-bold mt-4"><%= player.first_name %> <%= player.last_name %></h1>
      <p class="text-gray-600 mb-2 text-sm"><%= player.team.full_name %></p>
      <p class="text-gray-700 text-sm mb-4">Position: <%= player.position || 'N/A' %></p>

      <div class="grid grid-cols-3 gap-4 mt-4 text-sm">
        <div>
          <p class="font-semibold">Height</p>
          <p><%= player.height_feet || 'N/A' %>' <%= player.height_inches || '' %>"</p>
        </div>
        <div>
          <p class="font-semibold">Weight</p>
          <p><%= player.weight_pounds || 'N/A' %> lbs</p>
        </div>
        <div>
          <p class="font-semibold">PPG</p>
          <p><%= stats.ppg || 'N/A' %></p>
        </div>
        <div>
          <p class="font-semibold">APG</p>
          <p><%= stats.apg || 'N/A' %></p>
        </div>
        <div>
          <p class="font-semibold">RPG</p>
          <p><%= stats.rpg || 'N/A' %></p>
        </div>
      </div>

      <a href="/favorite-player" class="mt-6 inline-block bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700 transition">
        ← Back to Favorites
      </a>
    </div>
  <% } else { %>
    <p class="text-center text-gray-500">Player details not available.</p>
  <% } %>
</body>
</html>
